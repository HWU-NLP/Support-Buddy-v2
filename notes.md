tasks:
- [x] open a persistent connection to the background script

this repo is currently 'headless' with the UI sources in the v1 repo.

- [ ] improve the scraping with nested observers
    - [ ] find the primary column in the timeline
    - [ ] enumerate the tweets in the primary column

- [ ] track the ids of the tweets seen with decisions by the model
   - [ ] create a wasm module to enclose the data structure and lookup table











